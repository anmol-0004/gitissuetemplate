name: "🚀 New App Onboarding"
description: "Request to onboard a new application to FSAC shared cluster"
title: "ONBOARD: [App Name]"
labels: ["onboarding", "type: infra"]
assignees: []

body:
  - type: markdown
    attributes:
      value: |
        # New App Onboarding Request
        Complete all fields below. The platform team will use this to set up your app on FSAC.

  - type: textinput
    id: app_name
    attributes:
      label: "App Name"
      description: "Short, lowercase, unique identifier (e.g., payment-service, user-api)"
      placeholder: "my-app"
    validations:
      required: true

  - type: textinput
    id: team_name
    attributes:
      label: "Team Name"
      description: "Which team owns this app?"
      placeholder: "Backend Team, Platform Team, etc."
    validations:
      required: true

  - type: textarea
    id: app_description
    attributes:
      label: "App Description"
      description: "What does this app do? (1–2 sentences)"
      placeholder: "Handles payment processing for orders"
    validations:
      required: true

  - type: dropdown
    id: tech_stack
    attributes:
      label: "Primary Tech Stack"
      description: "What's the main language/framework?"
      options:
        - "Node.js / TypeScript"
        - "Python / FastAPI / Django"
        - "Go"
        - "Java / Spring Boot"
        - "C# / .NET"
        - ".NET / ASP"
        - "PHP"
        - "Ruby"
        - "Rust"
        - "Other"
    validations:
      required: true

  - type: textarea
    id: dependencies
    attributes:
      label: "External Dependencies"
      description: "e.g., databases (SQL, NoSQL), caches (Redis), messaging (RabbitMQ, Kafka), APIs, etc."
      placeholder: |
        - PostgreSQL
        - Redis
        - External API: https://example.com
    validations:
      required: false

  - type: dropdown
    id: environment
    attributes:
      label: "Initial Environment"
      description: "Where should this app be deployed first?"
      options:
        - "Dev only"
        - "Dev + Staging"
        - "Dev + Staging + Prod"
    validations:
      required: true

  - type: textarea
    id: expected_traffic
    attributes:
      label: "Expected Traffic & Resource Needs"
      description: "e.g., 100 RPS, peak 500 RPS; memory estimate; storage"
      placeholder: |
        - Peak traffic: 200 requests/sec
        - CPU: ~2-4 cores
        - Memory: ~2-4 GB
        - Storage: ~100 GB per month (logs)
    validations:
      required: true

  - type: textarea
    id: contact
    attributes:
      label: "On-Call Contact"
      description: "Team member responsible for this app in production (email + Slack handle)"
      placeholder: "jane@company.com (Slack: @jane)"
    validations:
      required: true

  - type: textarea
    id: additional_notes
    attributes:
      label: "Additional Notes"
      description: "Anything else the platform team should know? (special networking needs, custom monitoring, etc.)"
      placeholder: ""
    validations:
      required: false

  - type: markdown
    attributes:
      value: |
        ## Onboarding Checklist
        The platform team will complete the items below. Do not check them yourself.

  - type: checkboxes
    id: onboarding_steps
    attributes:
      label: "Platform Team: Complete these items"
      options:
        - label: "ONBOARD: Namespace creation + resource quota/limits"
          required: false
        - label: "ONBOARD: RBAC setup + AAD group mapping"
          required: false
        - label: "ONBOARD: Network policy + Istio sidecar injection"
          required: false
        - label: "ONBOARD: Ingress route + DNS entry"
          required: false
        - label: "ONBOARD: Sealed Secrets + secret rotation policy"
          required: false
        - label: "ONBOARD: ArgoCD Application + git repo registration"
          required: false
        - label: "ONBOARD: ServiceMonitor + Grafana dashboard + alert rules"
          required: false
        - label: "ONBOARD: Log aggregation pipeline"
          required: false
        - label: "ONBOARD: Backup policy (Velero schedule, if applicable)"
          required: false
        - label: "ONBOARD: Cost allocation labels + OpenCost tracking"
          required: false
        - label: "ONBOARD: Documentation + runbook links"
          required: false
        - label: "ONBOARD: Production sign-off + handoff"
          required: false